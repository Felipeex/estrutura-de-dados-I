#include <stdio.h>

int main() {
  // unidade de tempo = segundo
  // ler uma nova operação a cade 3 unidade de tempo.

  // tempo de espera = intervalo de tempo entre a leitura da nova operação, e a hora que ela foi inicializada.
  
  // apenas uma operação pode ser exercutada por vez.

  /* caso tenha uma operação sendo executada, e uma operação de prioridade maior for lida, 
  a operação que está sendo executada vai parar e voltar para fila. 
  Sendo assim, a operação com prioridade maior assumira a execução */
}